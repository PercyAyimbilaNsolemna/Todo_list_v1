<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>To Do List</title>
</head>
<body>

    <h1><%= kindOfDay %></h1>

    <ul>
        <li>Buy Food</li>
        <li>Cook Food</li>
        <li>Eat Food</li> 

        <% const listLength = nextItem.length; %> 

        <% if (listLength === 0) { %> 
            <li style="display: none;"><%= nextItem %></li>
        <% } else { %> 

            <% for (var i = 0; i < listLength; i++ ) { %> 

                <li><%= nextItem[i] %></li>

            <% } %> 
        <% } %> 
         
    </ul>

    <form action="/" method="post">
        <input type="text" name="nextItem" placeholder="Enter next item">
        <button type="submit">Add</button>
    </form>
</body>
</html>